# ============================================================================
# FlowyML Docker Compose Environment Configuration
# ============================================================================
# Copy this file to .env and customize values for your deployment

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================

# PostgreSQL credentials (change these in production!)
POSTGRES_USER=flowyml
POSTGRES_PASSWORD=flowyml_secure_password
POSTGRES_DB=flowyml

# Database URL (automatically constructed from above values)
# Override this to use external PostgreSQL or switch to SQLite
# FLOWYML_DATABASE_URL=postgresql://flowyml:flowyml_secure_password@postgres:5432/flowyml

# For SQLite (development only), uncomment this line:
# FLOWYML_DATABASE_URL=sqlite:////app/data/metadata.db

# ============================================================================
# Remote Server URLs
# ============================================================================

# Public URL for the backend API (used by CLI clients)
FLOWYML_REMOTE_SERVER_URL=http://localhost:8080

# Public URL for the frontend UI
FLOWYML_REMOTE_UI_URL=http://localhost:80

# For production with domain:
# FLOWYML_REMOTE_SERVER_URL=https://flowyml-api.yourcompany.com
# FLOWYML_REMOTE_UI_URL=https://flowyml.yourcompany.com

# ============================================================================
# Storage Configuration
# ============================================================================

# Directory for storing artifacts within the container
FLOWYML_ARTIFACTS_DIR=/app/artifacts

# Metadata DB path (only used if DATABASE_URL is SQLite)
FLOWYML_METADATA_DB=/app/data/metadata.db

# ============================================================================
# Authentication & Security
# ============================================================================

# Secret key for JWT token signing (REQUIRED IN PRODUCTION!)
# Generate with: openssl rand -hex 32
FLOWYML_AUTH_SECRET=change-this-secret-in-production

# Optional: Pre-configured API token for initial access
# FLOWYML_API_TOKEN=

# ============================================================================
# Application Environment
# ============================================================================

# Environment mode (production or development)
FLOWYML_ENV=production

# ============================================================================
# Cloud Storage (Optional)
# ============================================================================

# Uncomment and configure for S3 artifact storage
# FLOWYML_ARTIFACT_STORE=s3
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# FLOWYML_S3_BUCKET=flowyml-artifacts
# FLOWYML_S3_REGION=us-east-1

# Uncomment and configure for Google Cloud Storage
# FLOWYML_ARTIFACT_STORE=gcs
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
# FLOWYML_GCS_BUCKET=flowyml-artifacts

# Uncomment and configure for Azure Blob Storage
# FLOWYML_ARTIFACT_STORE=azure
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# FLOWYML_AZURE_CONTAINER=flowyml-artifacts
